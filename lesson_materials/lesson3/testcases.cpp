#include <bits/stdc++.h>

using namespace std;

// one test case:
int main(){
	int a,b; cin >> a >> b; // read in nums
	cout << a + b << " " << a * b << endl; // output result
}

// multiple test cases:
int main(){
	int tc; cin >> tc; // read in number of test cases

	// solve the problem for each test case
	for(int i = 0;i < n;i++){
		int a,b; cin >> a >> b;
		cout << a + b << " " << a * b << endl; 
	}
}
